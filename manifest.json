{
    "name": "documents",
    "type": "cli",
    "description": "Convert documents between formats. Accepts PDFs, Office files, and images. The `convert` command renders pages to PNG images. The `convert-to-markdown` command extracts text as Markdown. The `convert-markdown` command renders Markdown to Office/PDF outputs.",
    "command": "aech-cli-documents",
    "actions": [
        {
            "name": "convert",
            "description": "Render a document to page-by-page PNG images. Office files are converted to PDF first, then each page becomes `page_001.png`, `page_002.png`, etc. Returns JSON with the list of image paths.",
            "parameters": [
                {
                    "name": "input_path",
                    "type": "argument",
                    "required": true
                },
                {
                    "name": "output-dir",
                    "type": "option",
                    "required": true
                }
            ]
        },
        {
            "name": "convert-to-markdown",
            "description": "Extract document content as Markdown text. Legacy Office formats (.doc, .ppt, .xls) are upgraded first for better extraction. Returns JSON with the output Markdown file path.",
            "parameters": [
                {
                    "name": "input_path",
                    "type": "argument",
                    "required": true
                },
                {
                    "name": "output-dir",
                    "type": "option",
                    "required": true
                }
            ]
        },
        {
            "name": "convert-markdown",
            "description": "Render Markdown to Office/PDF outputs. Defaults to DOCX and PDF. Use `--format` (repeatable) to specify formats like docx, pdf, pptx. Returns JSON with the generated file paths.",
            "parameters": [
                {
                    "name": "input_path",
                    "type": "argument",
                    "required": true
                },
                {
                    "name": "output-dir",
                    "type": "option",
                    "required": true
                },
                {
                    "name": "format",
                    "type": "option",
                    "required": false,
                    "description": "Output format (docx, pdf, pptx, etc.). Can be repeated for multiple outputs."
                },
                {
                    "name": "reference-doc",
                    "type": "option",
                    "required": false,
                    "description": "Template file for styling Office outputs (docx/pptx)."
                }
            ]
        }
    ],
    "documentation": {
        "outputs": {
            "page_images": {
                "path": "<output-dir>/page_###.png",
                "description": "Numbered PNG pages from convert command."
            },
            "markdown_file": {
                "path": "<output-dir>/<stem>.md",
                "description": "Markdown file from convert-to-markdown command."
            },
            "converted_files": {
                "path": "<output-dir>/<stem>.<format>",
                "description": "Output files from convert-markdown command."
            }
        },
        "notes": [
            "All commands return JSON to stdout with output file paths.",
            "Use --output-dir to specify where files are written.",
            "Check command exit code and JSON output for success/failure."
        ]
    },
    "available_in_sandbox": true
}
