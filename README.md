# Agent Aech Documents CLI

This capability allows Agent Aech to ingest documents by converting them to images.

## Supported Formats
- PDF
- MS Office (DOCX, PPTX, XLSX, etc.)
- Images (PNG, JPG, etc.)

## Usage
```bash
python3 main.py convert <input_path> --output-dir <output_dir>
```
